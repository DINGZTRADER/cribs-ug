# API runtime
PORT=3000
NODE_ENV=development

# Database
DATABASE_URL="postgresql://rentflow:rentflow@localhost:5432/rentflow_db?schema=public"

# Auth
JWT_SECRET="dev_secret_key_change_in_prod"

# Optional integrations
MTN_API_KEY="change-me-if-used"
MTN_WEBHOOK_SECRET="change-me-if-used"
SUPABASE_URL="https://example.supabase.co"

# Dev-only module key (required only when using /dev/seed/search-data)
DEV_SEED_KEY="change-me-dev-only"
